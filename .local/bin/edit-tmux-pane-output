#!/bin/sh

if [ "$1" = "--scrollback" ]; then
  # capture full scrollback
  tmux capture-pane -e -p -S - | nvr - --ansi
else
  # capture just the visible pane
  tmux capture-pane -e -p | nvr - --ansi
fi
